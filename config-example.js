module.exports = {
    plivo: {
        sender: '', // the phone number on your account to send / call from
        authToken: '', // auth token from dashboard
        authId: '', // id from dashboard
        // this is for voice, you need to put some XML here. This is a real working example though, so be careful if you use it.
        answer_url: 'https://raw.githubusercontent.com/a-r-d/deadbeat-roommate/master/example-call-answered-xml.xml', 
    }
};